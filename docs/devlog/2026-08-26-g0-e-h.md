# 2026-08-26 — G0-E through G0-H project and relationship hardening

- Added project root policies for project/workspace/developer/tool/sdk/cache/vendor paths. Only verified Git fingerprints under promotable project roots can enter canonical `context/projects/`; SDK and cache candidates remain local evidence.
- Added bounded high-value executable discovery with explicit budget and candidate-only semantics. The real Discover run found 14 bounded candidates; Everything was correctly reported unavailable because no existing `es.exe` was found.
- Added semantic version/banner normalization for software and shell observations. A real Quick staging run normalized historical banners without copying user paths into proposed canonical JSON.
- Added strong relationship derivation for verified installation paths and promotable project manifest fingerprints. A real Discover staging run derived 8 relationships and passed validation without publishing.

All runs used read-only collection and `-NoPublish`; canonical files were not changed by these audits.
