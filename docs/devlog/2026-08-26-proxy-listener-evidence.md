# 2026-08-26 — proxy listener evidence

The read-only listener check found `127.0.0.1:7988` owned by a process named `FlClashCore`. No listener was present on the historical `10808`, `10100`, or `18080` ports. The process executable path was not available without expanding permissions.

This is useful evidence that the current local proxy path is associated with the FlClash family, but it does not prove the user's intended primary client, the relationship to `E:\FlClash\FlClash-0.8.96`, or any proxy policy. The evidence remains in ignored `.local/audit-closure.json`; no canonical project promotion or curated network constraint was made.
