# Schemas

Formal machine-readable schemas are optional in the earliest V1 work. `SCHEMA.md` currently defines the lightweight human-readable contract.

If validation complexity grows, place JSON Schema or equivalent files here for machine, software, project, relationship, and manifest records. Keep schema evolution additive where possible and bump `schema_version` only for genuine breaking changes.
