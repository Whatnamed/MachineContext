# Test fixtures

Store only synthetic or deliberately sanitized fixtures here.

Fixtures may model command output, registry/query results, path-resolution edge cases, missing tools, and malformed records. Never copy real credentials, proxy subscriptions, auth files, browser data, or unreviewed machine dumps into this directory.
