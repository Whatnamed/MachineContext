meta:
  state: bootstrap
  verification:
    state: pending
    source: detected
    observed_at: null

# Copy this file only when a real software domain needs its own module.
# Register the new module in context/software/index.yaml.
software: []
