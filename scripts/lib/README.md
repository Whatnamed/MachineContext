# Script library

Shared deterministic helpers belong here: command detection, path normalization, verification metadata, YAML-safe normalization, stable ordering, and privacy-safe utilities.

Keep system calls and parsing/normalization separable where practical so the latter can be tested with fixtures.
